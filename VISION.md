> **LOCKED governing file.** Do not edit in place. See `GOVERNANCE.md`.

# Vision

No thesis has been given yet. Every slot below is `TODO(Dhruv)` deliberately — **do not invent a
thesis to fill these in.** A plausible fabrication is far more expensive to detect later than an
obvious blank: an agent working from a fabricated thesis builds the wrong thing confidently. Tracked
as **Q-0002** in `research/open_questions.md`.

## What we're building

TODO(Dhruv)

## For whom

TODO(Dhruv) — who is the user/beneficiary of this project? Be specific (a role, a team, a persona),
not "everyone."

## Why it isn't already solved

TODO(Dhruv) — what gap does this fill that existing tools/products don't? If this hasn't been
checked yet, that's a job for prior-art research (see `research/prior_art.md`) before committing to
build.

## What "working" looks like at demo time

TODO(Dhruv) — the concrete, observable state that counts as success when judges watch the demo. This
should be specific enough to become the happy path in `DEMO.md` once an idea is chosen.

## Non-goals

TODO(Dhruv) — what this project explicitly will NOT attempt, so scope doesn't creep under deadline
pressure. Even a placeholder list of "obvious adjacent things we are not doing" is useful once the
idea firms up.

## Relationship to event facts

This vision must fit inside whatever `COMPETITION.md` turns out to specify (event identity, rubric,
hard rules) once Q-0001 is resolved. Do not lock in a thesis that assumes an event identity (e.g. an
onchain thesis) before Q-0001 is answered.
